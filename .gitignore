# Secrets — never commit
.env
.env.*
!.env.example

# Foundry
/contracts/out/
/contracts/cache/
/contracts/broadcast/

# Node
node_modules/

.vercel
.env*

# Rollback snapshots of Vercel env (may contain secrets) — never commit
.rollback/
