import { StubScreen } from "@/components/app/StubScreen";

export default function CreatePage() {
  return (
    <StubScreen
      title="Create"
      body="The 3-step launch flow (Project → Treasury → Review) lands here. It needs the BallastFactory, which is blocked on verifying the modified UniversalRouter encoding before any pool is deployed."
    />
  );
}
