import { StubScreen } from "@/components/app/StubScreen";

export default function ProfilePage() {
  return (
    <StubScreen
      title="Profile"
      body="Creator track record anchored to the X account ID, treasury history, and retention land here. Needs X OAuth and the indexer."
    />
  );
}
