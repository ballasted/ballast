import { StubScreen } from "@/components/app/StubScreen";

export default function PortfolioPage() {
  return (
    <StubScreen
      title="Portfolio"
      body="Holdings, P&L, and a backed-vs-unbacked exposure bar land here. Needs wallet holdings plus per-token backing from BackingLens."
    />
  );
}
