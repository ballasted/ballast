# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# Downloaded, checksum-verified solc binary cache (see scripts/lib/compile.ts)
scripts/.solc-cache/
.vercel
.env*

# Ad-hoc verification/screenshot scripts — never committed, never risk
# clobbering something real (see Milestone 1 retro: a scratch script
# overwrote an existing untracked file at the repo root instead of here).
.scratch/
